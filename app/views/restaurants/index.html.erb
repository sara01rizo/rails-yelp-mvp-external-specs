<div class="container">
  <h1>Hello from index</h1>
</div>
<div class="container">
  <div class="row">
    <div class="d-flex align-items-center">
      <div class="col-10"><h1 class="mt-3">Restaurants 🌮</h1></div>
      <div class="col-2"> <%= link_to "Create new Restaurant", new_restaurant_path, class: "btn btn-success"%></div>
    </div>
  </div>
<div class="wrapper mt-5">
  <% @restaurants.each do |restaurant| %>
    <div class="restaurant-card">
      <div class="text-container">
        <h4 class="animate-gradient-text"><%= restaurant.name %></h4>
        <h6>Address</span> <br> <%= restaurant.address  %></h6>
      </div>
    </div>
  <% end %>
  </div>
</div>
